customtkinter
Pillow
# 0.5.0 的 macOS arm64 只带 Tcl 9 版 tkdnd(libtcl9tkdnd*.dylib),在 Tk 8.6 下报
# "incompatible stubs mechanism" 崩溃。0.4.2 仍带 Tcl 8.6 兼容的 libtkdnd2.9.3.dylib。
tkinterdnd2==0.4.2
requests
# main.py 显式用它的证书文件修复打包版 SSL_CERT_VERIFY_FAILED（见 main.py 注释）。
# requests 本来就带了这个传递依赖，这里转成直接依赖只是让它别悄悄消失。
certifi
pywin32; sys_platform == 'win32'
# Qt 截图预览子进程（Retina 高清标注窗）。Essentials 只含 QtCore/QtGui/QtWidgets
# 等核心模块，比完整 PySide6 小得多。缺失时 app 自动回退 Tk 标注窗（模糊但可用）。
# 锁版本保持 CI 与本地一致（曾因版本漂移误诊打包体积问题，真凶是 zip 不加 -y
# 展开 .app 内 symlink，与 PySide6 版本无关）。升级前本地打包验证一遍即可。
PySide6-Essentials==6.10.3